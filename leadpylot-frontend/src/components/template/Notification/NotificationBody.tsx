/**
 * NotificationBody - DEPRECATED
 *
 * This component has been replaced by NotificationTableBody (Monday.com-style inbox).
 * The drawer in Notification.tsx now uses NotificationTableBody directly.
 *
 * Kept as a minimal stub for backward compatibility in case any imports reference it.
 * Safe to delete once all references are confirmed removed.
 */

const NotificationBody = () => null;

export default NotificationBody;
