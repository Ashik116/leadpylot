/**
 * Pagination Constants
 * 
 * Centralized configuration for pagination defaults across the application.
 * Change these values to update pagination behavior globally.
 */

/**
 * Default page size/limit for pagination
 * This is the default number of items displayed per page throughout the application
 */
export const DEFAULT_PAGE_LIMIT = 80;

/**
 * Default page number (usually 1 for first page)
 */
export const DEFAULT_PAGE = 1;

/**
 * Common page size options for pagination dropdowns
 * These are the standard options users can select from
 */
export const DEFAULT_PAGE_SIZE_OPTIONS = [10, 25, 50, 100];

/**
 * Extended page size options (for cases where more options are needed)
 */
export const EXTENDED_PAGE_SIZE_OPTIONS = [10, 20, 50, 100, 200, 500, 1000];

